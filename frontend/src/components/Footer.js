import React from "react";

const Footer = () => {
    return (
        <footer className="footer">
            <p>&copy; {new Date().getFullYear()} Event Polling App.</p>
        </footer>
    );
};

export default Footer;
